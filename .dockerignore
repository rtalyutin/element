# Exclude dependency directories from the build context
node_modules
**/node_modules

# Ensure dependency manifests remain available for Docker builds
!spa/package.json
!spa/package-lock.json

# Exclude local debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
